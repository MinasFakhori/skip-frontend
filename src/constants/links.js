// One place to store all the links used in the app, so that they can be easily changed in the future
const Links = Object.freeze({
    OPENSTREETMAP_TILE: "https://tile.openstreetmap.org/{z}/{x}/{y}.png",
});

export default Links;